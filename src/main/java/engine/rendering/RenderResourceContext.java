package engine.rendering;

import engine.rendering.resources.ResourceHandle;

public class RenderResourceContext {
    public RenderResourceContext() {
    }

    public void allocate(ResourceHandle resource) {
        // Need to clone this resource and save it in a list
    }

    public void deallocate(ResourceHandle resource) {

    }
}
